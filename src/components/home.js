import React from 'react';
import { Icon, Layout, Row, Col, Button } from 'antd';

const Home = (props) => {
  return (
    <div className="home-page">
      Hello World
    </div>
  );
}

export default Home;